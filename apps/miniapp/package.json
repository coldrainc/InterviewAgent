{
  "name": "interview-agent-miniapp",
  "version": "0.1.0",
  "private": true,
  "description": "WeChat Mini Program shell for Interview Agent",
  "scripts": {
    "check": "node --test tests/*.test.js && node scripts/check-structure.js"
  }
}
